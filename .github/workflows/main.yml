name: Build MAMM
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Ejecutar Tecnologia MAMM
        uses: ArtemSBulgakov/buildozer-action@v1
        with:
          command: buildozer android debug
          workdir: .
      - name: Entrega de APK
        uses: actions/upload-artifact@v4
        with:
          name: MAMM-APK
          path: ./bin/*.apk
